- hosts: localhost

  vars:

    homebrew_installed_packages:
      - go
      - protobuf

    homebrew_cask_apps:
      - goland
      - sequel-pro
      - java
      - slack

  roles:
    - geerlingguy.homebrew
