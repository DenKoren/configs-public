- hosts: localhost

  vars:

    homebrew_installed_packages:
      - go@1.12
      - protobuf
      - bats-core
      - npm
      - shellcheck
      - kubectl
      - python3

    homebrew_cask_apps:
      - goland
      - sequel-pro
      - java
      - slack

  roles:
    - geerlingguy.homebrew
