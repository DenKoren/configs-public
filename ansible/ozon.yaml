- hosts: localhost

  vars:

    homebrew_installed_packages:
      - go
      - protobuf
      - bats-core

    homebrew_cask_apps:
      - goland
      - sequel-pro
      - java
      - slack

  roles:
    - geerlingguy.homebrew
